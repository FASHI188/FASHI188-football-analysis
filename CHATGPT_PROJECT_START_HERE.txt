【足球项目 START HERE｜FAST_BOOTSTRAP V3 + EXECUTION_LITE V1｜跨项目唯一稳定启动器】

用途：负责新对话、新项目、刷新、断线后的快速接续与轻量化路由；不保存当前Rxx/HEAD/run/state_version等动态状态，不替代唯一正式CURRENT。

固定身份：
- project_id: football-project
- GitHub: FASHI188/FASHI188-football-analysis
- Airtable Base: 足球项目接续（appLXF9IBvSCEUjJV）

一、默认启动只做3个小读取
1. Airtable《当前状态》唯一激活记录，只取接续必要字段；
2. 按该记录里的“执行检查点记录ID”精确读取那一条《执行检查点》；
3. 只核验GitHub main当前HEAD SHA。

以下四项一致即FAST_BOOTSTRAP_PASS：project_id、state_version、checkpoint_version、GitHub main HEAD=Airtable保存main SHA。

通过后：
- “到哪了/接上了吗”→直接用当前状态+检查点回答；
- “继续/开始/接着干/执行”→从检查点“恢复位置/唯一下一步”继续，不重述、不重做。

二、跨项目永久接续
ChatGPT项目只是工作窗口，不是唯一状态存储。

若“足球2”或任何当前足球项目再次出现对话满、项目异常或需要换新项目：
1. 新项目不要复制旧项目聊天历史；
2. 新项目继续使用同一个project_id、GitHub仓库和Airtable Base；
3. 第一轮只执行上面的3个小读取；
4. FAST_BOOTSTRAP_PASS后直接从最新检查点继续；
5. 旧项目聊天、旧项目记忆只允许按需作为历史证据，绝不能成为恢复前提。

因此，“足球2→足球3→足球4”不会形成不同项目状态；它们只是不同工作窗口，外部唯一状态始终是football-project。

三、只有异常或任务需要才扩大读取
仅在main SHA漂移、版本冲突、RUNNING/VERIFYING副作用核验、state发布/修复、或当前任务明确需要证据时，才按需读取LAST_HANDOFF.md、PROJECT_CURRENT.md、绑定维护日志、指定PR/run/job/Artifact。

禁止启动时：搜索全部项目聊天、列全部维护日志、列全部PR/Artifact、遍历整个Library/仓库、回放旧Rxx。

四、CURRENT硬门
任务涉及正式预测、模型研究、训练、评分、晋级、正式概率/比分/EV或修改CURRENT时，在实际执行前必须完整读取项目来源中唯一文件名包含“CURRENT_唯一正式规则”的CURRENT；数量不等于1或无法完整读取立即停止。

五、历史研究按需查
问Rxx→精确搜Airtable维护日志中的Rxx；问某机制→用1—3个关键词定向搜；命中后只展开相关记录及其GitHub证据。除非用户明确要求全量审计，不加载全部历史。

六、EXECUTION_LITE执行链硬门
FAST_BOOTSTRAP通过后，连续外部工具执行必须遵守GitHub `EXECUTION_LITE.md`：
- FAST_BOOTSTRAP的3个小读取不计执行块预算；
- 每个后续执行块最多6次外部工具调用、最多1次重型读取、最多1个主要副作用动作；
- 到第5次调用仍需新的重型读取时，先写WAITING/适当终态断点，再进入下一块；
- 同一用户回合默认最多连续2个执行块；
- 已知record ID/path/HEAD/PR/run/job/Artifact ID必须精确读取，窄任务禁止递归整仓库树、整表扫描、整Actions历史和重复connector schema discovery；
- ZIP/raw bytes、大批数据、大日志、大Artifact、批量模型计算优先放GitHub Actions/容器，聊天只做冻结输入、启动/定位、精确核验和checkpoint；
- 出现输出截断、连续connector错误/超时、或下一步明显是重型查询时，先形成安全WAITING断点，禁止继续无限叠加调用。

七、长任务
拆成可恢复执行块；每块开始/结束更新执行检查点。连续完成两个实质块、下一步明显是大日志/大Artifact/批量处理，或已出现上下文/输出截断时，先形成WAITING安全断点，再换新对话。新对话或新项目只需说“继续”。

八、文件原始字节
Library/Project中“可搜索”不等于“可处理原始字节”。ZIP/CSV等如工具没有raw-byte路径，必须取得可用原始字节来源；不得假装已经读取。

九、固定边界
- 旧聊天、旧文件、记忆只能作历史证据，不得覆盖CURRENT或当前GitHub/Airtable状态；
- 普通“继续”不扩大研究、标签、训练、Provider、PR Ready/合并或正式资产权限；
- 详细接续规则以GitHub AGENTS.md、ACTIVE_CHECKPOINT.md与EXECUTION_LITE.md为准；FAST_BOOTSTRAP正常通过时不需要每次全文读取它们。

十、强制落盘门
- 任何研究结果只要改变当前瓶颈、研究主方向、优先级、停止条件或“唯一下一步”，必须在同一工作回合先更新Airtable《执行检查点》；构成重要方向变化时必须同时发布新state_version；
- 不允许等到对话快满才补接力；长任务每个实质执行块结束都要保存“上一已完成/恢复位置/唯一下一步/禁止重复”；
- 状态未持久化前，禁止声称“新对话/新项目可以直接接上”；若持久化失败，必须明确标记HANDOFF_NOT_PERSISTED；
- 新对话/新项目恢复时更高state_version永远覆盖更低state_version；同一state下更高checkpoint_version覆盖更低checkpoint。

十一、未来新项目的最小初始化文本
只需要让新项目知道这一条：
“这是football-project。先按GitHub `CHATGPT_PROJECT_START_HERE.txt`执行FAST_BOOTSTRAP；Airtable Base为`足球项目接续`（appLXF9IBvSCEUjJV），仓库为`FASHI188/FASHI188-football-analysis`。不要读取或搬运旧项目聊天历史。”

之后所有动态进度都从外部状态恢复，不依赖旧项目记忆。
