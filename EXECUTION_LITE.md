# 足球项目执行链轻量化协议｜EXECUTION_LITE V2

本文件只治理实际执行阶段的工具负载与副作用安全，不保存动态项目状态，也不建立第二套接续系统。

它不改变唯一正式 CURRENT、模型、数据、研究预注册、样本、科学结论、formal_weight 或权限边界。

## 1. 启动边界

动态施工状态只读 Airtable《当前状态》唯一激活记录。旧 checkpoint / handoff / pointer 不参与执行恢复。

需要正式预测、模型研究、训练、评分、晋级、正式概率/比分/EV或正式资产修改时，另按 `AGENTS.md` 读取唯一正式 CURRENT。

## 2. 精确读取优先

已知精确锚点时优先直接读取：record ID、GitHub path、branch/HEAD、PR、run/job、Artifact、关键词命中对象。不要为一个窄问题默认展开整表、整库、全部PR历史、全部Actions或大Artifact。

只有用户明确要求全量审计，或精确路径无法定位时，才扩大查询范围。

## 3. 执行块与上下文控制

一个执行块只完成一个明确目标及必要验收。大批CSV/JSON、ZIP、数百场/数千场计算、大日志和可重复批处理优先放到GitHub Actions、容器或专门执行环境；聊天侧负责冻结输入、启动/定位、精确核验和记录结论。

不要把大对象全文复制进聊天或维护日志；保留必要结论及HEAD/PR/run/job/Artifact/SHA等证据锚点。

## 4. 写操作与断线安全

产生外部副作用前：

1. 确认用户当前明确指令覆盖该动作；
2. 精确核验目标对象当前状态；
3. 优先使用可幂等、可核验的最小写入；
4. 写后立即回读真实结果。

若调用超时、连接中断或返回不完整：不得盲目重试。先查询目标对象，判断副作用是否已经发生；已发生则禁止重复，明确未发生才安全重试，无法判断则停止并报告歧义。

不再为了每个工具调用创建或更新 Airtable《执行检查点》。

## 5. 状态更新

只有重要项目状态真实变化时，才更新 Airtable《当前状态》并向《维护日志》追加一条历史记录。维护日志不授予执行权限，也不成为恢复入口。

高频只读核验、普通解释和未改变项目状态的操作不制造 state 噪声。

## 6. 权限

普通“继续”不能自动获得付费Provider、Secret、新盲测标签、正式训练/评分/晋级、CURRENT/模型/权重/config修改、PR Ready/merge、扩大样本或研究预算等权限。

《当前状态》为 STOPPED / WAITING_USER / IDLE 时，普通“继续”不得发明新Rxx或恢复历史日志中的任务。

## 7. 验收原则

- 精确读取优先，不做无边界扫描；
- 大工作下放执行环境；
- 每个写操作都有明确当前授权与写后核验；
- 断线后先查真实副作用，不靠旧checkpoint猜；
- 当前施工状态始终只由 Airtable《当前状态》决定；
- 历史证据与当前执行权严格分离。
