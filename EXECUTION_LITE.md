# 足球2 执行链轻量化协议｜EXECUTION_LITE V2

只治理工具/连接器/上下文负载，不改变CURRENT、模型、数据、科学合同或权限。

## 1. 前置条件
必须先 BOOT3_PASS。任何写操作还必须持有当前 pointer 的 `写入锁ID`。若锁不属于当前授权操作，只读停止。

## 2. 执行块硬预算
BOOT3 三个小读取不计预算。每个执行块默认：
- 最多6次外部工具调用；
- 最多1次重型读取；
- 最多1个主要副作用目标；
- 只闭合一个明确阶段。

同一用户回合默认最多2个实质执行块。完成2块后，除非只剩一次很小的终态核验，否则先把pointer写成安全恢复态再进入下一回合/新对话。

## 3. 精确读取优先
已知 record ID/path/branch/HEAD/PR/run/job/Artifact ID 时直接精确读取。禁止为找一个对象列整表、整Actions历史、递归仓库树、整File Library或全部聊天。
同一事实本回合已有 verified SHA/ID 不重复读，除非副作用或漂移要求。

## 4. 重型读取
以下视为重型：大日志全文、大Artifact下载/展开、ZIP/raw bytes、大CSV/JSON、递归树、整表/整PR历史、批量模型结果。
优先放 GitHub Actions/容器处理，执行环境输出小型 summary/manifest：输入身份、SHA、样本数、参数、状态、关键指标、Artifact ID、错误摘要。
聊天侧只读summary，禁止把大对象全文复制回上下文。

## 5. 副作用与幂等
副作用前：回读 pointer_version + 写入锁ID + target。副作用后：先核验真实状态，再更新pointer/#210。
如果超时/断线/返回不完整，不直接重试；先查副作用：已发生则补状态，明确未发生且可安全重试才用同一operation重试；无法判断 `SIDE_EFFECT_AMBIGUOUS` 并BLOCKED。

## 6. 主动收口/换对话门
任一条件触发：
- 当前块到第5次调用仍需要新的重型读取；
- 工具输出发生截断/压缩；
- 连续2次connector错误/超时；
- 已完成2个实质块；
- 下一步是大日志、大Artifact、批处理或另一个独立重阶段；
- pointer已经足以无损恢复；
- 当前对话明显很长，继续留在旧对话没有额外信息优势。

触发时：完成当前可安全原子步骤 → 更新pointer和#210 → 返回 `CONTEXT_HANDOFF_REQUIRED`。新对话只需“继续”。不得为了“把这一轮做完”继续无限叠加工具调用。

## 7. 防止项目上下文自我膨胀
- 新对话不主动搜索旧项目聊天；
- 不把PROJECT_CURRENT/LAST_HANDOFF/旧checkpoint作为默认boot输入；
- 不在维护日志和聊天重复粘贴大Artifact/代码/日志；
- 历史问题按Rxx/关键词/ID精确检索；
- 动态状态只保留在单一pointer；
- 长研究尽早切换，不等平台提示“对话已满”。

平台自身可能自动注入项目历史，治理无法物理缩小产品上下文窗口；因此我们的硬策略是**不再主动叠加历史**，并在已有安全pointer时提前换对话。

## 8. 回执最小化
每块只向聊天写：做了什么、真实结果、证据锚点、阻塞、唯一下一步。详细内容留GitHub/Airtable/Artifact。

## 9. 权限不扩展
普通“继续”不授权新研究、新标签、Provider、正式模型/CURRENT、Ready/merge等。权限由当前用户明确指令 + pointer授权摘要机械裁决。
