# 足球项目执行链轻量化协议｜EXECUTION_LITE V1

本文件只治理 ChatGPT/连接器/GitHub/Airtable 在“实际执行阶段”的调用方式，目标是降低长工具链、重查询、大对象展开导致的连接中断风险。

它不改变唯一正式 CURRENT、模型、数据、研究预注册、样本、科学结论、formal_weight 或任何权限边界。

## 1. 适用范围

FAST_BOOTSTRAP 通过后，只要任务需要连续调用外部工具、读取 GitHub/Airtable、跑 workflow、处理 Artifact/大文件或跨系统核验，就进入 EXECUTION_LITE。

启动接续与执行治理分工：
- FAST_BOOTSTRAP：解决“新对话怎么快速接上”；
- EXECUTION_LITE：解决“接上后怎么避免一口气把执行链拖得过重”。

## 2. 执行块硬预算

FAST_BOOTSTRAP 的3个小读取不计入执行块预算。

每个后续执行块默认硬限制：
- 最多 6 次外部工具调用；
- 最多 1 次重型读取；
- 最多 1 个主要副作用动作；
- 一个块只完成一个明确目标及其必要验收。

“重型读取”包括但不限于：递归仓库树、大型日志全文、大 Artifact 下载/展开、整表扫描、整 PR/Actions 历史、全库搜索或会返回明显大对象的调用。

到第5次调用时若目标仍不能安全闭合：不得再开启新的重型读取；先把已完成事实、恢复位置、禁止重复和唯一下一步写入 Airtable《执行检查点》，形成 WAITING/适当终态，再进入下一执行块。

同一用户回合默认最多连续完成 2 个执行块。达到2块后，除非只剩一次小型结果核验，否则必须先形成安全 WAITING 断点再继续下一回合/新对话。

## 3. 精确读取优先级

只要存在精确锚点，就禁止用更大的发现型查询替代：

1. 已知 Airtable record ID → 直接读该记录，禁止列整表找它；
2. 已知 GitHub path → `fetch_file` 精确读文件，禁止递归整仓库树；
3. 已知 branch/HEAD → 精确读该 branch/commit，禁止搜索全部分支；
4. 已知 PR/run/job/Artifact ID → 精确取该对象，禁止列全部历史；
5. 已知关键词/错误码 → 先定向 find/search，再只展开命中对象；
6. 已有本轮已验证事实与SHA锚点 → 不重复读取同一事实，除非发生漂移或副作用核验。

## 4. 明确禁止的重查询模式

窄任务下默认禁止：
- `git/trees?...recursive=1` 式整仓库递归树遍历；
- 已经知道工具/字段结构后重复加载完整 connector schema；
- 为找一条记录而列整个 Airtable 表；
- 为找一个 workflow/job 而列全部 Actions 历史；
- 为确认一个文件而遍历整个 File Library/项目来源；
- 同一执行块重复展开相同大日志、大JSON或大Artifact；
- 因“可能相关”而无边界地打开旧 Rxx/PR/聊天历史。

只有用户明确要求“全量审计”，或精确路径确实不存在且无法通过小范围搜索定位时，才允许重查询；执行前必须先写检查点并把该重查询视为本块唯一重型读取。

## 5. connector/schema 复用

同一对话中某个连接器动作/字段 schema 已经成功发现后，应复用现有定义，不得为了同一动作重复 `list_resources`/完整 schema discovery。

只有以下情况允许重新发现：
- 工具调用明确返回动作不存在/参数结构改变；
- 新任务需要此前未发现的另一类动作；
- 对话已切换且当前运行环境没有可复用定义。

## 6. 大工作下放

以下工作优先放到 GitHub Actions、容器或专门的数据执行环境，不在聊天工具链里逐条展开：
- ZIP/raw bytes 下载与 SHA；
- 大批 CSV/JSON 处理；
- 数百场/数千场批量计算；
- 大日志解析；
- 大 Artifact 重建；
- 可重复的模型/研究批处理。

聊天侧只负责：冻结输入/参数 → 启动或定位执行 → 精确核验 run/job/Artifact/摘要 → 把结论与恢复锚点写进 checkpoint。

优先让执行环境输出小型 manifest/summary，至少包含：输入身份、SHA、样本数、关键参数、状态、关键指标、Artifact ID/路径和错误摘要。

## 7. 写操作与断线安全

任何可能产生外部副作用的操作前，必须先把检查点设为 RUNNING 并写入操作ID、目标对象、预期副作用和禁止重复。

若调用超时、客户端断线、回复被截断或工具返回不完整：
1. 不直接重试写操作；
2. 新连接先读取当前状态+精确检查点；
3. 按操作ID/目标对象核验副作用是否已发生；
4. 已发生则补写完成状态，禁止重复；
5. 明确未发生且可安全重试，才重试同一操作ID；
6. 无法判断则 BLOCKED_CHECKPOINT_SIDE_EFFECT_AMBIGUOUS。

## 8. 主动停止阈值

出现任一情况，当前块必须尽快收口为 WAITING/适当终态，而不是继续叠加调用：
- 已用到本块第5次外部调用且仍需新的重型查询；
- 工具输出出现截断/压缩；
- 连续两次 connector 错误、超时或连接中断；
- 下一步需要递归树、大日志、大Artifact或批量处理；
- 已完成两个执行块；
- 当前检查点已经足以让新对话无损恢复。

## 9. 结果回执最小化

执行过程不把大对象复制回聊天。回执只保留：
- 做了什么；
- 真实结果；
- 精确证据锚点（HEAD/PR/run/job/Artifact/record ID/SHA）；
- 当前阻塞；
- 唯一下一步。

详细原文留在 GitHub/Airtable/Artifact，不重复塞进聊天上下文。

## 10. 与 CURRENT / 权限的关系

EXECUTION_LITE 只降低执行负载，不扩大权限。

普通“继续”仍不能自动获得：付费 Provider、Secret、新盲测标签、正式训练/评分/晋级、CURRENT/模型/权重/config 修改、PR Ready/merge、扩大样本或研究预算等权限。

涉及正式预测、模型研究、训练、评分、晋级、正式概率/比分/EV或 CURRENT 修改，仍必须先通过唯一正式 CURRENT 硬门。

## 11. 验收标准

EXECUTION_LITE 生效后，一个正常研究回合应满足：
- 启动仍只走 FAST_BOOTSTRAP；
- 执行不做无边界全库/整树/整表扫描；
- 大工作放执行环境，聊天只核验小结果；
- 每个实质块都有可恢复 checkpoint；
- 断线后能从精确 checkpoint 恢复且不重复副作用；
- 即使平台仍可能偶发掉线，也不会因为聊天自身无限扩张工具链而把项目拖入不可恢复状态。
