# ACTIVE_CHECKPOINT｜LEGACY / DEPRECATED BY CONTINUITY V3

本文件自 V3 起**不再是新对话启动源，也不保存实时执行权威**。

实时动态执行真源已迁移到 Airtable《唯一接续指针》唯一 active record；GitHub issue #210仅作为镜像。

旧《执行检查点》记录和本协议保留历史审计/幂等证据，不删除、不改写历史事实，但不得覆盖当前pointer。

新对话固定走：Airtable唯一pointer → GitHub #210 mirror → 实际branch/HEAD/PR/run。任何不一致 `POINTER_MISMATCH`。

详细规则：`governance/CONTINUITY_PROTOCOL_V3.md`。
