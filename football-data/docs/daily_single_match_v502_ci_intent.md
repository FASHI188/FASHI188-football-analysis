# CI intent

The CI run is the first full-repository execution of this branch. Any failure will be treated as a development defect, repaired on the same branch, and rerun without invoking Codex between iterations.
