# Execution policy

The committed workflow executes only repository-contained code and data. It never supplies an API key, never calls API-Football, and never fetches match data. The fixed offline scenario is an auditable integration test, not a fixture recommendation.
