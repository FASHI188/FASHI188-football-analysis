# Audit note

All generated evidence is ephemeral and content-hashed. The workflow checks exact HEAD identity and uploads the generated bundle as a 30-day Artifact. It does not persist generated probability output into the repository.
