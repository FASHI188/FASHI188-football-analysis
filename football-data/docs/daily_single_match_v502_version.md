# Version binding

The wrapper reports CURRENT `V5.0.2` and delegates probability generation to the existing formal V4.6.x engine registered by the CURRENT chain. The distinction is explicit in every output: CURRENT governs policy and output; the existing engine supplies the audited joint probability matrix.
