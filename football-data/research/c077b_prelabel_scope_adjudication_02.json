{
  "schema_version": "C077B_PRELABEL_SCOPE_ADJUDICATION_02",
  "stage": "PRELABEL_ZERO_VALUE_ONLY",
  "numeric_score_values_opened": false,
  "reason": "The zero-label PASS manifest still contained norway/2025_nocup.txt. The project default settlement is 90 minutes plus stoppage only; a cup file can contain knockout fixtures whose published final result semantics may be ambiguous with respect to extra time. Before any score value is decoded, remove the cup file rather than later adjudicating outcomes using result values.",
  "remove_source_file": "norway/2025_nocup.txt",
  "selection_basis": "source path and competition type only; no numeric score, total goals, goal difference, T>=7 membership, prediction or loss used",
  "expected_effect_on_zero_label_inventory": "remove only the cup-file identities and recompute the deterministic manifest/SHA; all frozen scientific confirmation thresholds remain unchanged",
  "unchanged_scientific_gates": true,
  "hard_boundary": "After this scope correction, rerun the zero-label gate and freeze the new exact identity SHA before any numeric score access."
}
