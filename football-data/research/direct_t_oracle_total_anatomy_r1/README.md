# Direct-T oracle total-state anatomy R1

Purpose: post-view diagnosis of whether PR #179's clean same-fit Direct-T oracle heterogeneity is organized by realized total-goal state.

This is not a deployable selector experiment. Realized `total_class` is used only after the exact 1,584-row PR #179 evaluation and frozen experts are reconstructed. The diagnostic reports:

- per-total-class expert mean losses and row-oracle winner counts;
- each total bucket's contribution to the full static-to-oracle LogLoss gap;
- an ex-post true-T bucket router ceiling;
- a coarse 0-3 vs 4+ true-T router ceiling;
- the fraction of the full row-level oracle gap captured by those non-deployable label-informed routers.

Hard boundary: retrospective VIEWED only; selector fit=0; threshold search=0; new data/provider/paid API/protected sample consumption=0; formal_weight=0; no formal model/data/config/CURRENT/main mutation; no promotion claim.
