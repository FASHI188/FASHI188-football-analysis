# Legacy R41 reproducibility seal — 2026-08-14

Research governance only. `formal_weight=0`. Formal model/data/config/CURRENT changes = 0.

## Decision

Legacy R41 is sealed as **exploratory evidence that cannot be re-opened as the same candidate**.

The project retains the recorded preregistration SHA-256:

`eea489b77ca4c89a9ab44681599c636f5dbc8d4df60040825798670b860fede1`

However, the complete original preregistration body could not be recovered from the repository or the project handoff records. The persisted material is sufficient to preserve the historical result, but not sufficient to prove the exact original feature transforms, full executable algorithm, fitted-parameter contract, and decision rule required for an unchanged-candidate confirmation.

Therefore:

- `legacy_r41_reproducibility_gate = FAIL_ORIGINAL_PREREG_NOT_RECOVERED`
- `legacy_r41_label_opening_authorized = false`
- `legacy_r41_same_candidate_replication_allowed = false`
- the historical 44.0% natural Top-1 Draw precision is **not** a prior, threshold, promotion gate, or formal performance estimate;
- no new sample may be labeled and described as an independent confirmation of legacy R41;
- no old injury-onset experiment, R40 static-availability candidate, or approximate reconstruction may substitute for legacy R41.

## Historical evidence preserved without reinterpretation

The only surviving project-level scientific summary is:

- consumed exploratory rows: 609 across 2023/24 and 2024/25;
- global HDA LogLoss delta: `+0.002107`;
- Draw AUC delta: `-0.002534`;
- global probability-layer gate: failed;
- natural Top-1 Draw predictions: 25;
- natural Top-1 Draw hits: 11;
- natural Top-1 Draw precision: 44.0%;
- natural Top-1 Draw recall: 8.27%;
- no forced Top-k and no post-result threshold search were recorded.

This remains exploratory evidence only.

## Corrective action

A new candidate is registered separately as `R41R2_PIT_AVAILABILITY_TRANSITION_RESIDUAL_R1` in:

`football-data/research/r41r2_pit_availability_transition_prereg_20260814.json`

R41R2 is a new candidate in the same hypothesis family, **not a reconstruction of legacy R41**. It must have its own exact identity, frozen feature definitions, algorithm, data gates, evaluation gates, and future result. Legacy R41's 44.0% result cannot be inherited by R41R2.

## 2025/26 untouched confirmation data status

The data-availability problem is separately resolved under zero-label audit:

- sparse vaastav history: 128 eligible, below the inherited 150-row gate — FAIL;
- high-frequency `Randdalf/fplcache` source audit: 380/380 — PASS;
- kickoff-identity re-audit using latest score-free fixture identity at least 6h pre-kickoff: 380/380 — PASS;
- confirmation identity manifest SHA-256: `4e22607eee2f0f4729f26cfee826bedac74a3ce5012160a1643c46429a69860e`;
- target labels / target scores / training / scoring / tuning during these gates: 0.

Those 2025/26 labels remain closed by this seal. R41R2 preregistration itself also does not authorize opening them.
