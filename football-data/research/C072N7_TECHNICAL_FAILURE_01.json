{
  "schema": "C072N7_TECHNICAL_FAILURE_01",
  "run_id": 32244394749,
  "job_id": 96041665790,
  "scientific_or_source_verdict": "NOT_AVAILABLE",
  "classification": "POST_EXECUTION_EVIDENCE_PERSISTENCE_FAILURE",
  "failure": "After the frozen five-league evaluator completed its network/request loop and reached persist(result), the safety assertion `assert \"nonce_value\" not in text.lower()` matched the evaluator's own metadata key `nonce_values_persisted_or_logged`. The assertion fired before the result JSON was written or printed, so no counts/source verdict are recoverable from the run artifact/log.",
  "request_recipe_change_allowed": false,
  "gate_change_allowed": false,
  "known_boundary_facts": {
    "contract_guard_passed": true,
    "failure_step": "persist(result) after evaluator main logic",
    "target_result_values_materialized_by_design": 0,
    "football_row_values_persisted_by_design": 0,
    "nonce_values_persisted_by_design": 0,
    "model_fit": 0,
    "formal_weight": 0,
    "C073_C077_quarantined": true
  },
  "adjudication": "Do not interpret N7 as PASS or FAIL. Do not retry inside N7. A separately frozen engineering replay may rerun the exact same five one-row metadata requests and exact same frozen gates solely because the first run lost its output after execution. The replay may change only the evidence-persistence safety check so it checks serialized values/forbidden secret fields without matching a benign metadata key name."
}
