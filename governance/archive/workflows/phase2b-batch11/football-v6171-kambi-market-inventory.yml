name: football-v6171-kambi-market-inventory
on:
  workflow_dispatch:
  push:
    paths:
      - "football-data/validation/v6_kambi_market_criterion_inventory_v6171.py"
      - ".github/workflows/football-v6171-kambi-market-inventory.yml"
permissions:
  contents: read
jobs:
  audit:
    permissions:
      contents: write
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v6
        with: {fetch-depth: 0}
      - uses: actions/setup-python@v6
        with: {python-version: "3.12"}
      - run: python -m py_compile football-data/validation/v6_kambi_market_criterion_inventory_v6171.py
      - run: python football-data/validation/v6_kambi_market_criterion_inventory_v6171.py
      - name: Persist receipt
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPOSITORY: ${{ github.repository }}
        run: |
          python football-data/validation/persist_generated_worktree_v474.py \
            --allow-prefix football-data/manifests/v6_kambi_market_criterion_inventory_v6171_status.json \
            --message-prefix "audit(football): persist V6.17.1 Kambi market inventory"
