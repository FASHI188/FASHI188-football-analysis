name: Football V6.16.5a AH Line Scan
on:
  push:
    branches: [main]
    paths:
      - "football-data/validation/audit_ah_line_distribution_v6165a.py"
      - ".github/workflows/football-v6165a-ah-line-scan.yml"
  workflow_dispatch:
permissions:
  contents: write
jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: python football-data/validation/audit_ah_line_distribution_v6165a.py
      - shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          p=football-data/manifests/v6_ah_line_distribution_v6165a.json
          git add "$p"
          if ! git diff --cached --quiet; then
            git commit -m "audit(football): persist V6.16.5a AH line scan"
            git pull --rebase origin main
            git push origin HEAD:main
          fi
