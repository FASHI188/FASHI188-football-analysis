name: Football V6.16.1 Nonlinear Total and Conditional Score
on:
  push:
    branches: [main]
    paths:
      - "football-data/validation/validate_hgb_total_conditional_score_v6161.py"
      - ".github/workflows/football-v6161-hgb-total-score.yml"
  workflow_dispatch:
permissions:
  contents: write
jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: python -m pip install --quiet scikit-learn
      - run: python -m py_compile football-data/validation/validate_hgb_total_conditional_score_v6161.py
      - env:
          PYTHONPATH: football-data/engine:football-data/validation
        run: python football-data/validation/validate_hgb_total_conditional_score_v6161.py
      - shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          p=football-data/manifests/v6_hgb_total_conditional_score_v6161_status.json
          if [ -f "$p" ]; then git add "$p"; fi
          if ! git diff --cached --quiet; then
            git commit -m "audit(football): persist V6.16.1 nonlinear total-score challenger"
            git pull --rebase origin main
            git push origin HEAD:main
          fi
