name: Football V4.7 Dynamic Strength Stage Adapter

on:
  push:
    branches:
      - main
    paths:
      - "football-data/manifests/dynamic_strength_stage_route_audit_v470_status.json"
      - "football-data/validation/dynamic_strength_stage_adapter_gate_v470.py"
      - ".github/workflows/football-v470-dynamic-strength-stage-adapter.yml"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: football-v470-dynamic-strength-stage-adapter
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: python -m py_compile football-data/validation/dynamic_strength_stage_adapter_gate_v470.py
      - run: python football-data/validation/dynamic_strength_stage_adapter_gate_v470.py
      - name: Commit adapter receipt
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add football-data/manifests/dynamic_strength_stage_adapter_v470_status.json
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "audit(football): persist explicit dynamic-strength stage adapter status"
          git pull --rebase origin main
          git push origin HEAD:main
