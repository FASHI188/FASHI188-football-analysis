name: Football R39K Recovery Superseded

on:
  pull_request:
    branches: [research/market-draw-regime-r39j]
    paths:
      - '.github/workflows/football-r39k-recovery.yml'

permissions:
  contents: read

concurrency:
  group: r39k-recovery-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  supersede-duplicate-recovery:
    runs-on: macos-latest
    steps:
      - run: echo 'Duplicate recovery lane superseded; authoritative recovery is football-r39k-evaluation.yml.'
