name: Football Beat The Bookie Identity Lock R39B FROZEN

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  frozen-consumed-fixed100:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'R39B/R39C first fixed100 identity lock is consumed and frozen; do not relock or replace it.'
