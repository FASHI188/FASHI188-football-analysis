name: Football R40I0 Result Identity Audit FROZEN

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  frozen-failed-r40i0:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'R40I0 authoritative run 31406052238 is frozen as STOP_R40I0_RESULT_IDENTITY_AUDIT.'
          echo 'Frozen external eligible result coverage was 100%, but the full result table contained 160 duplicate match_id values.'
          echo 'Score/outcome values accessed: 0. Duplicate eligible identity audit must occur before result-value access.'
