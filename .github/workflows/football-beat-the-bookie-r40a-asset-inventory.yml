name: Football Beat The Bookie R40A Asset Inventory FROZEN

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  frozen-canonical-zero-row-inventory:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'Canonical R40A asset inventory run: 31395388838.'
          echo 'Artifact 9065394441; archive contains five tabular assets and no OU/AH/BTTS/lineup fields by header.'
          echo 'Do not rerun this completed zero-row source inventory.'
