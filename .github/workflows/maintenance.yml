# PAUSED_FOR_REPOSITORY_GOVERNANCE
name: Governance Maintenance Skeleton

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: governance-maintenance-${{ github.ref }}
  cancel-in-progress: true

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo "PAUSED_FOR_REPOSITORY_GOVERNANCE"
