name: C079-A2.4 Full DataTables Odds Shape Probe
# trigger-only; schema-probe boundary unchanged
on:
  push:
    branches: [research/c079a2-footiqo-endpoint-audit-20260820]
    paths:
      - research/probe_c079a24_full_datatables_shape.py
      - .github/workflows/c079a24_full_dt_shape.yml
permissions:
  contents: read
jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with: {python-version: '3.11'}
      - run: python -m pip install requests==2.32.5 beautifulsoup4==4.13.5 lxml==6.0.1
      - run: |
          mkdir -p "$RUNNER_TEMP/out"
          python research/probe_c079a24_full_datatables_shape.py --out-dir "$RUNNER_TEMP/out"
      - name: Enforce market-only schema probe boundary
        run: |
          python - <<'PY'
          import json,os
          from pathlib import Path
          s=json.loads((Path(os.environ['RUNNER_TEMP'])/'out'/'summary.json').read_text())
          assert s['schema_version']=='C079A24_FULL_DT_SHAPE_V1'
          b=s['hard_boundary']; assert b['requested_rows_per_odds_table']==1 and b['only_odds_tables_requested'] is True
          for k in ['odds_row_values_persisted','score_result_table_requested','score_values_parsed','result_targets_computed','authentication_attempted','premium_bypass_attempted','model_fit']:
              assert b[k] is False,(k,b[k])
          print(json.dumps(s,ensure_ascii=False,indent=2))
          PY
      - uses: actions/upload-artifact@v4
        with:
          name: c079a24-full-datatables-shape
          retention-days: 90
          path: |
            ${{ runner.temp }}/out/summary.json
            research/probe_c079a24_full_datatables_shape.py
