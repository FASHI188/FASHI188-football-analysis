name: Football Beat The Bookie R39C Evaluation FROZEN

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  frozen-consumed-fixed100:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'R39C fixed100 evaluation is permanently frozen after its one completed evaluation.'
          echo 'Do not rerun or reuse the R39C locked100 for tuning.'
