name: Football R40H Cross Source Transfer Freeze FROZEN

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  frozen-failed-r40h:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'R40H authoritative run 31404048650 is frozen as STOP_R40H_CROSS_SOURCE_FREEZE.'
          echo 'Reason: three conflicting same-match/same-date_created external odds records triggered the preregistered fail-closed gate.'
          echo 'External result values accessed: 0. Do not rerun or reinterpret R40H as PASS.'
