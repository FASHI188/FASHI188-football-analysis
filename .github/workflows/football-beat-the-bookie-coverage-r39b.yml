name: Football Beat The Bookie Coverage R39B FROZEN

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  frozen-completed-stage:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'R39B coverage stage is completed and frozen; do not rerun from later PR synchronization.'
