name: Football Beat The Bookie R39E Bookmaker Continuity FROZEN

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  frozen-canonical-zero-label-audit:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'Canonical R39E bookmaker continuity run: 31390873687.'
          echo 'Artifact: 9063687995; choice USE_ALL_HOURS_COMMON_BOOKMAKER_SET_MIN3.'
          echo 'Do not rerun this completed no-label audit.'
