name: Football V4.7 Online Draw Residual Rolling OOF

on:
  push:
    branches:
      - main
    paths:
      - "football-data/validation/validate_draw_online_residual_rolling_oof_v470.py"
      - "football-data/validation/validate_draw_residual_rolling_oof_v470.py"
      - "football-data/validation/validate_draw_residual_rolling_oof_v470_strict.py"
      - "football-data/validation/screen_draw_residual_challenger_v470.py"
      - "football-data/validation/screen_draw_residual_challenger_v470_strict.py"
      - "football-data/validation/backtest_last_complete_season_all_domains_v470.py"
      - ".github/workflows/football-v470-draw-online-residual-rolling-oof.yml"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: football-v470-draw-online-residual-rolling-oof
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Compile
        run: |
          python -m py_compile football-data/validation/backtest_last_complete_season_all_domains_v470.py
          python -m py_compile football-data/validation/screen_draw_residual_challenger_v470.py
          python -m py_compile football-data/validation/screen_draw_residual_challenger_v470_strict.py
          python -m py_compile football-data/validation/validate_draw_residual_rolling_oof_v470.py
          python -m py_compile football-data/validation/validate_draw_residual_rolling_oof_v470_strict.py
          python -m py_compile football-data/validation/validate_draw_online_residual_rolling_oof_v470.py
      - name: Run online residual rolling OOF
        id: validate
        shell: bash
        env:
          PYTHONPATH: football-data/engine:football-data/validation
        run: |
          set +e
          python football-data/validation/validate_draw_online_residual_rolling_oof_v470.py
          rc=$?
          echo "rc=$rc" >> "$GITHUB_OUTPUT"
          exit 0
      - name: Commit online residual receipt
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add football-data/manifests/draw_online_residual_rolling_oof_v470_status.json
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "research(football): persist online draw residual rolling OOF"
          git pull --rebase origin main
          git push origin HEAD:main
      - name: Preserve validation failure after receipt persistence
        if: steps.validate.outputs.rc != '0'
        run: exit 1
