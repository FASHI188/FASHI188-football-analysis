name: Football Beat The Bookie R39C Preflight FROZEN

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  frozen-completed-stage:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'R39C preflight is completed and frozen; later research must use a new preregistration and holdout.'
