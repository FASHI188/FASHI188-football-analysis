name: Football R40H0 External Conflict Quarantine FROZEN

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  frozen-passed-r40h0:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'R40H0 authoritative run 31404861780 passed.'
          echo 'Three conflicting rows belonged to one match outside the frozen R40G eligible population.'
          echo 'R40G 21,561 identities, split counts and hashes remain unchanged. External result values accessed: 0.'
