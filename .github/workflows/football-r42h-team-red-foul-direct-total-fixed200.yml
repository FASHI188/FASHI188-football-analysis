name: Football R42H Team Red-Foul Direct-T Fixed200

on:
  pull_request:
    paths:
      - ".github/workflows/football-r42h-team-red-foul-direct-total-fixed200.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: football-r42h-team-red-foul-direct-total-fixed200-${{ github.ref }}
  cancel-in-progress: true

jobs:
  frozen-during-pretest:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm scoring freeze
        run: |
          echo "R42H target scoring frozen pending no-target-label pretest diagnostic."
          echo "No target labels, model scoring, or scientific metrics are executed by this workflow."
