name: football-v6174-v6164-random100
on:
  workflow_dispatch:
  push:
    paths:
      - "football-data/validation/v6_joint_ipf_random100_baseline_v6174.py"
      - ".github/workflows/football-v6174-v6164-random100.yml"
permissions:
  contents: read
jobs:
  audit:
    permissions:
      contents: write
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6
        with: {fetch-depth: 0}
      - uses: actions/setup-python@v6
        with: {python-version: "3.12"}
      - run: python -m py_compile football-data/validation/v6_joint_ipf_random100_baseline_v6174.py
      - run: python football-data/validation/v6_joint_ipf_random100_baseline_v6174.py
      - name: Persist receipt
        if: always()
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPOSITORY: ${{ github.repository }}
        run: |
          python football-data/validation/persist_generated_worktree_v474.py \
            --allow-prefix football-data/manifests/v6_joint_ipf_random100_baseline_v6174_status.json \
            --message-prefix "audit(football): persist V6.17.4 V6.16.4 random100 baseline"
