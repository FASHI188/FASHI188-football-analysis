name: Football V4.7 Market Coordination Runtime Smoke

on:
  push:
    branches:
      - main
    paths:
      - "football-data/engine/market_coordination_runtime_v470.py"
      - "football-data/validation/smoke_market_coordination_runtime_v470.py"
      - ".github/workflows/football-v470-market-coordination-runtime-smoke.yml"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: football-v470-market-coordination-runtime-smoke
  cancel-in-progress: true

jobs:
  smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Compile
        run: |
          python -m py_compile football-data/engine/market_coordination_runtime_v470.py
          python -m py_compile football-data/validation/smoke_market_coordination_runtime_v470.py
      - name: Run smoke
        env:
          PYTHONPATH: football-data/engine:football-data/validation
        run: python football-data/validation/smoke_market_coordination_runtime_v470.py
      - name: Commit smoke receipt
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add football-data/manifests/market_coordination_runtime_v470_smoke.json
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "audit(football): persist market coordination runtime smoke"
          git pull --rebase origin main
          git push origin HEAD:main
