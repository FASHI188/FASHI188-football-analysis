name: Football R40I0A Result Duplicate Audit FROZEN

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  frozen-failed-r40i0a:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'R40I0A authoritative run 31406567539 is frozen as STOP_R40I0A_DUPLICATE_RESULT_ID_AUDIT.'
          echo 'Exactly one duplicate result match_id intersects the frozen eligible set; it is in fit only. Policy and blind have zero duplicated result IDs.'
          echo 'Score/outcome values accessed: 0.'
