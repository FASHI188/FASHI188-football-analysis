name: football-v6-prelineup-v645
on:
  workflow_dispatch:
  push:
    paths:
      - "football-data/validation/v6_prelineup_stability_v645.py"
      - "football-data/validation/v6_prelineup_stability_v645_r2.py"
      - ".github/workflows/football-v6-prelineup-v645.yml"
permissions:
  contents: read
jobs:
  validate:
    permissions:
      contents: write
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6
        with: {fetch-depth: 0}
      - uses: actions/setup-python@v6
        with: {python-version: "3.12"}
      - run: |
          python -m py_compile football-data/validation/v6_prelineup_stability_v645.py
          python -m py_compile football-data/validation/v6_prelineup_stability_v645_r2.py
      - run: python football-data/validation/v6_prelineup_stability_v645_r2.py
      - name: Persist
        if: always()
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPOSITORY: ${{ github.repository }}
        run: |
          python football-data/validation/persist_generated_worktree_v474.py \
            --allow-prefix football-data/manifests/v6_prelineup_stability_v645_r2_status.json \
            --message-prefix "audit(football): persist V6.4.5 r2 pre-lineup stability pilot"
      - name: Enforce
        if: always()
        run: |
          python - <<'PY'
          import json
          from pathlib import Path
          p=Path('football-data/manifests/v6_prelineup_stability_v645_r2_status.json')
          if not p.exists(): raise SystemExit('V645 r2 missing')
          x=json.loads(p.read_text())
          if x.get('status')!='PASS': raise SystemExit(x.get('status'))
          print(json.dumps({'audit':x.get('data_audit'),'gain':x.get('accuracy_gain_pp'),'guard':x.get('proper_score_guard'),'gate':x.get('pilot_gate_passed')},ensure_ascii=False))
          PY
# Research pilot only; no CURRENT/formal/runtime mutation.
