# PAUSED_FOR_REPOSITORY_GOVERNANCE
name: Governance Scheduled Data Skeleton

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: governance-scheduled-data-${{ github.ref }}
  cancel-in-progress: true

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo "PAUSED_FOR_REPOSITORY_GOVERNANCE"
