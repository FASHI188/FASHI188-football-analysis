name: football-v6-zero-modified-skellam-v672
on:
  workflow_dispatch:
  push:
    paths:
      - "football-data/validation/v6_zero_modified_skellam_draw_v672.py"
      - ".github/workflows/football-v6-zero-modified-skellam-v672.yml"
permissions:
  contents: read
jobs:
  validate:
    permissions:
      contents: write
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6
        with: {fetch-depth: 0}
      - uses: actions/setup-python@v6
        with: {python-version: "3.12"}
      - run: python -m py_compile football-data/validation/v6_zero_modified_skellam_draw_v672.py
      - run: python football-data/validation/v6_zero_modified_skellam_draw_v672.py
      - name: Persist
        if: always()
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPOSITORY: ${{ github.repository }}
        run: |
          python football-data/validation/persist_generated_worktree_v474.py \
            --allow-prefix football-data/manifests/v6_zero_modified_skellam_draw_v672_status.json \
            --message-prefix "audit(football): persist V6.7.2 structural zero-mass draw"
      - name: Enforce
        if: always()
        run: |
          python - <<'PY'
          import json
          from pathlib import Path
          p=Path('football-data/manifests/v6_zero_modified_skellam_draw_v672_status.json')
          if not p.exists(): raise SystemExit('V672 receipt missing')
          x=json.loads(p.read_text())
          if x.get('status')!='PASS': raise SystemExit(str(x))
          print(json.dumps({'selected':x.get('selected_candidate'),'holdout':x.get('challenger_holdout'),'gate':x.get('research_gate_passed'),'interpretation':x.get('interpretation')},ensure_ascii=False))
          PY
# Research challenge only; no CURRENT/formal/runtime mutation.
