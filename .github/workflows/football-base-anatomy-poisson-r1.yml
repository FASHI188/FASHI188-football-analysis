name: Football Base Anatomy Poisson R1

on:
  push:
    branches:
      - research/base-anatomy-20260817
    paths:
      - "scripts/run_base_anatomy_poisson_r1.py"
      - ".github/workflows/football-base-anatomy-poisson-r1.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: football-base-anatomy-poisson-r1-${{ github.ref }}
  cancel-in-progress: false

jobs:
  stage1-poisson:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - name: Compile research runner
        run: python -m py_compile scripts/run_base_anatomy_poisson_r1.py
      - name: Stage-1 old formal Poisson bottom
        run: python scripts/run_base_anatomy_poisson_r1.py --bootstrap-resamples 5000
      - name: Upload research artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: base-anatomy-poisson-r1
          path: |
            football-data/research/base_anatomy_20260817/poisson_base_r1.json
            football-data/research/base_anatomy_20260817/poisson_base_r1.md
          if-no-files-found: error
