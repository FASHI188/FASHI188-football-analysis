name: Football Beat The Bookie R39E Sequence Coverage FROZEN

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  frozen-canonical-zero-label-audit:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'Canonical R39E sequence coverage run: 31390215828.'
          echo 'Artifact: 9063504606; result PASS_R39E_FULL_PREMATCH_SEQUENCE_COVERAGE_NO_LABELS.'
          echo 'Do not rerun this completed aggregate no-label audit.'
