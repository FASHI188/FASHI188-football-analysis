name: Football Betfair Draw Trajectory Pilot Prereg R2 Edit Guard

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  edit-guard:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Preregistration amendment in progress; no experiment or data access is permitted."
