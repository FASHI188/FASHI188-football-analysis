# Football Project Mandatory Instructions

Before performing any football-project task:

1. Read this `AGENTS.md` completely.
2. Read the current authoritative rule at `football-data/audit/football_draw_signal_closure_audit_v503.md`.
3. Read the latest handoff and bind all work to the actual current HEAD.
4. Distinguish `PROVEN`, `COMPUTED`, `INFERRED`, `UNPROVEN`, and `NOT_AUTHORIZED`.
5. Never convert a test PASS, workflow Success, Artifact upload, coverage result, candidate, or preregistration into a broader business conclusion.
6. If required evidence is missing, stop and report `UNPROVEN`.
7. Training, holdout access, provider requests, secret access, formal-weight changes, formal-asset changes, and PR merge require separate explicit user authorization.
8. If instructions conflict, use the stricter evidence boundary.
9. Do not silently rewrite previously validated rule versions.
10. Report skipped and unexecuted paths explicitly.
